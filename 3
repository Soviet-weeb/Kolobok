import numpy as np
'''3. Створіть масив з п'яти прізвищ і виведіть їх на екран стовпчиком,
починаючи з останнього.'''
A=np.array(['Jotaro','Fuji','Bucke','Down','Bold'])
n=len(A)
for i in range(-1,-n-1,-1):     #Старт = -1( для счета с конца), стоп = -n-1, шаг=-1, для обхода массива с конца.
    print(A[i])
